#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -eq 1 ] && [ "$1" = "--help" ]; then
  echo "Usage: at-submit"
  exit 0
fi

if [ "$#" -ne 0 ]; then
  echo "Usage: at-submit" >&2
  exit 1
fi

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/submit"
